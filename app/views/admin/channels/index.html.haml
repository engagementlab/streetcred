%h1 Channels

.well
  Channels should include all services that will make calls to the StreetCred API. Service providers will need a valid StreetCred API key in order to save actions or read information.

%table.table
  %tr
    %th Name
    %th API Key
    %th Actions
    %th
    %th

  - @channels.each do |channel|
    %tr
      %td= channel.name
      %td= channel.key
      %td= channel.actions.try(:count)
      %td{width: 35}= link_to 'Edit', edit_admin_channel_path(channel), :class => 'btn btn-mini'
      %td{width: 55}= link_to 'Destroy', admin_channel_path(channel), :method => :delete, :data => { :confirm => 'Are you sure?' }, :class => 'btn btn-mini btn-danger'


= link_to 'New Channel', new_admin_channel_path, :class => 'btn btn-success'
