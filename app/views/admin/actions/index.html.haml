%h1 Actions
%p.well
  Actions are created through the StreetCred API by submitting a POST request to http://streetcred.herokuapp.com/api/actions.json?params

%table.table
  %tr
    %th Action Type
    %th Source
    %th User Email
    %th Created At
    %th
  - @actions.each do |action|
    %tr
      %td= action.action_type
      %td= action.source
      %td= action.user.try(:email)
      %td= action.created_at
      %td{width: 40}= link_to 'Show', admin_action_path(action), :class => 'btn btn-mini'
