.page.container

  %h2 Current Campaigns
  .active-campaigns
    - for campaign in @active_campaigns
      = render partial: 'campaign', locals: {campaign: campaign}

  %h2 Completed Campaigns
  .completed-campaigns
    - for campaign in @completed_campaigns
      = render partial: 'campaign', locals: {campaign: campaign}